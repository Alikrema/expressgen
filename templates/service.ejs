const <%- singularName %>Repository = require('../repos/<%- singularName %>Repo');

const get<%- singularCamelCaseName %>ById = async (id) => {
  try {
    return await <%- singularName %>Repository.getById(id);
  } catch (error) {
    throw error;
  }
};

const getAll<%- singularCamelCaseName %>s = async () => {
  try {
    return await <%- singularName %>Repository.getAll();
  } catch (error) {
    throw error;
  }
};

const create<%- singularCamelCaseName %> = async (data) => {
  try {
    return await <%- singularName %>Repository.create(data);
  } catch (error) {
    throw error;
  }
};

const update<%- singularCamelCaseName %> = async (id, data) => {
  try {
    return await <%- singularName %>Repository.update(id, data);
  } catch (error) {
    throw error;
  }
};

const delete<%- singularCamelCaseName %> = async (id) => {
  try {
    return await <%- singularName %>Repository.delete(id);
  } catch (error) {
    throw error;
  }
};

module.exports = {
  get<%- singularCamelCaseName %>ById,
  getAll<%- singularCamelCaseName %>s,
  create<%- singularCamelCaseName %>,
  update<%- singularCamelCaseName %>,
  delete<%- singularCamelCaseName %>
};
