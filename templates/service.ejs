const <%- name %>Repository = require('../repos/<%- name %>Repos'); 

const get<%- name %>ById = async (id) => {
  try {
    return await <%- name %>Repository.getById(id);
  } catch (error) {
    throw error;
  }
};

const getAll<%- name %>s = async () => {
  try {
    return await <%- name %>Repository.getAll();
  } catch (error) {
    throw error;
  }
};

const create<%- name %> = async (data) => {
  try {
    return await <%- name %>Repository.create(data);
  } catch (error) {
    throw error;
  }
};

const update<%- name %> = async (id, data) => {
  try {
    return await <%- name %>Repository.update(id, data);
  } catch (error) {
    throw error;
  }
};

const delete<%- name %> = async (id) => {
  try {
    return await <%- name %>Repository.delete(id);
  } catch (error) {
    throw error;
  }
};

module.exports = {
  get<%- name %>ById,
  getAll<%- name %>s,
  create<%- name %>,
  update<%- name %>,
  delete<%- name %>
};
